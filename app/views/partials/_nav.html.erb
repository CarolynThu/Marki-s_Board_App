<nav class="navbar navbar-inverse" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Marki's Boards</a>
      <%#= link_to "Marki's Boards", main_path %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Gallery", new_board_path %></li>
        <li><a href="/about">About Marki</a></li>
      </ul>
    
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
        <p class="navbar-text">Hello, <%= current_user.name %></p>
        <% end %>

        <li>
          <% if current_user && current_user.is_admin %>
          <%= link_to 'Admin Only', boards_path %>
          <% else %>
          
          <% end %>
        </li>
        <li>
          <% if current_user %>
          <%= link_to 'Your Account', user_path(current_user.id) %>
          <% else %>
          <% end %>
        </li>
        <li>
          <% if current_user %>
          <%= link_to 'Edit Profile', edit_user_path(current_user.id) %>
          <% else %>
           <a href="#" data-toggle="modal" data-target="#signup_modal">Sign up</a>
           <% end %>
        </li>
        <li>
          <% if current_user %>
          <%= link_to 'Logout', session_path, method: :delete %>
          <% else %>
          <!-- Button trigger modal -->
        
            <a href="#" data-toggle="modal" data-target="#myModal" >Log in</a>
          <% end %>
        </li>









  
       

      </ul>
    </div><!-- /.navbar-collapse --
  </div><!-- /.container-fluid -->
</nav>